# Copy to .env and fill the values as needed
BASE_URL=https://reqres.in/api
# Optional; if your API uses bearer token auth
API_TOKEN=
# Optional; API key header (e.g., for reqres)
X_API_KEY=reqres-free-v1
# Optional; set to 'true' to send Authorization header
USE_AUTH=false
